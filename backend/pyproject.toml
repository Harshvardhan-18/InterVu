[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "intervu-backend"
version = "0.1.0"
description = "InterVu AI Interview Platform — Backend"
requires-python = ">=3.11"
